# GP-J4.3 — Modelo de Receita: Taxa de Sucesso

> Jornada: GP Comercial J4 · Guarda-chuva: [GP-J4](J4-orcamentacao-modelos.md)
> Estado auditado: ✅ IMPLEMENTADO · Complexidade: **Baixa–Média** (existe; expor no seletor + campos de apuração)
> Dependências externas: validação de margem mínima depende de Admin J4 (Tsuru) — transversal, ver guarda-chuva

## Estado Atual (auditado)

**✅ Já desenvolvido:**
- `BillingType = 'success_fee'` em `src/types/service.ts`
- Cálculo `calculateSuccessFeeTotals`; campos `success_fee_percent`, `success_fee_type` (`'pontual' | 'continuo'`) em `src/types/budget.ts`
- Wizard `WIZARD_STEPS_BY_TYPE.success_fee` e detalhe

**❌ Pendente:**
- Expor/garantir o modelo no **novo seletor "Modelo de Receita"**
- Campos de **critério de medição**, **período de apuração** e **valor mínimo garantido (opcional)** — confirmar se já existem; senão, adicionar

## História de Usuário

**Como** GP Comercial,
**quero** orçar uma **taxa de sucesso** (% sobre resultado) com critério e período de apuração,
**para que** eu feche uma proposta atrelada a resultado com as regras de medição claras.

## Plano de Orçamento (Critérios de Aceite)

**CA-01 — Seleção do modelo**
- "Taxa de Sucesso" selecionável no dropdown "Tipo de modelo"; aciona o wizard `success_fee`.

**CA-02 — Campos do orçamento**
- **% sobre o resultado** (`success_fee_percent`) e **tipo** (`pontual` / `contínuo`)
- **Critério de medição** (sobre o quê incide)
- **Período de apuração**
- **Valor mínimo garantido** (opcional)

**CA-03 — Cálculo e margem**
- `calculateSuccessFeeTotals` estima a receita e **margem bruta %** no `MarginGauge` (com base estimada do resultado)

**CA-04 — Detalhe e persistência**
- `BudgetDetail.tsx` exibe %, tipo, critério e período
- Grava `billing_type = 'success_fee'` e campos, respeitando `tenant_id`/RLS
- Rascunho incompleto salvo e marcado como "rascunho"

## Fora do Escopo
- Validação de margem contra `minimum_margin` (transversal — ver guarda-chuva; depende de Admin J4)
- Equity, Indicação e combinações (tasks próprias)

## Notas Técnicas
- Tipos: `success_fee` em `service.ts`; `success_fee_percent`/`success_fee_type` em `budget.ts`
- Wizard `BudgetForm.tsx`; cálculo `calculateSuccessFeeTotals`; detalhe `BudgetDetail.tsx`
- Se faltarem critério/período/valor mínimo, ampliar `budget.ts` sem quebrar o cálculo atual

## Critérios de Teste

| Cenário | Resultado esperado |
|---|---|
| Selecionar "Taxa de Sucesso" no seletor | Wizard de taxa de sucesso abre |
| % + critério + período de apuração | Campos salvos e exibidos no detalhe |
| Valor mínimo garantido preenchido | Considerado na estimativa/exibição |
| Margem bruta | `MarginGauge` mostra a % estimada |
| Rascunho incompleto | Salvo e marcado como "rascunho" |
| Tenant diferente | RLS impede ver/salvar de outro tenant |
